/* DATAtable */
$(document).ready(function() {
    $('#example').DataTable({
    
        
       
    });
    
});

/* alert xóa */
function Del(name){
    return confirm ("Bạn có chắc muốn xóa : " + name + " ?");
}